{
  "name": "claude-manager",
  "version": "2.0.0",
  "description": "Full orchestration tool for Claude Code development - manage profiles, automate workflows, track sessions, and accelerate AI development",
  "type": "module",
  "bin": {
    "cm": "./dist/cli.js",
    "claude-manager": "./dist/cli.js"
  },
  "scripts": {
    "build": "esbuild src/cli.js --platform=node --format=esm --loader:.js=jsx --bundle --outfile=dist/cli.js --packages=external && echo '#!/usr/bin/env node' | cat - dist/cli.js > dist/tmp && mv dist/tmp dist/cli.js && chmod +x dist/cli.js",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "claude",
    "claude-code",
    "anthropic",
    "ai",
    "cli",
    "mcp",
    "profile-manager",
    "orchestration",
    "workflow",
    "automation",
    "session-management",
    "task-queue",
    "development-tools",
    "ai-development"
  ],
  "author": "Faisal Nazir",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/faisalnazir/claude-manager.git"
  },
  "homepage": "https://github.com/faisalnazir/claude-manager#readme",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "fuse.js": "^7.1.0",
    "ink": "^5.1.0",
    "ink-select-input": "^6.0.0",
    "ink-text-input": "^6.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
